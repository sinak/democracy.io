import * as React from "react";
import MessageForm from "./MessageForm";

test("should require all input fields", () => {

});
test("submit - calls API /message");
test("renders the legislator's topics");
